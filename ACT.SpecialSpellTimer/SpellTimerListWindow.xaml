<Window 
    x:Class="ACT.SpecialSpellTimer.SpellTimerListWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="SpellTimerListWindow" SizeToContent="WidthAndHeight"
    AllowsTransparency="True" WindowStyle="None" Opacity="1" Background="{x:Null}"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextHintingMode="Fixed"
    TextOptions.TextRenderingMode="ClearType">

    <Grid Name="BackgroundColorGrid">

        <Canvas
            Margin="0,0,0,0">
            <Rectangle
                x:Name="BaseColorRectangle"
                Margin="0,0,0,0"
                Fill="Transparent"
                RadiusX="8"
                RadiusY="8"
                Width="{Binding ActualWidth, ElementName=BackgroundColorGrid}"
                Height="{Binding ActualHeight, ElementName=BackgroundColorGrid}"
                />
        </Canvas>

        <Grid Name="BaseGrid" Margin="8,6,6,6">
        </Grid>

    </Grid>

</Window>
    